#!/usr/bin/env sh
set -eu

echo "Sprintable 1.3.2 build is maintained through build.ps1 because MCP 7.2/Forge setup uses Windows batch scripts in this workspace." >&2
echo "Run from PowerShell:" >&2
echo "  ./build.ps1 [-McpDir ../mcp72] [-SkipMcpBuild]" >&2
exit 1
