#!/usr/bin/env sh
set -eu

echo "Sprintable 1.4.7 build is maintained through build.ps1 because MCP 726a/Forge setup uses Windows batch scripts in this workspace." >&2
echo "Run from PowerShell:" >&2
echo "  ./build.ps1 [-McpDir ../mcp726a] [-SkipMcpBuild]" >&2
exit 1
